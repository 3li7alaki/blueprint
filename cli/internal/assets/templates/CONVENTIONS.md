# Conventions

<!-- budget: 80 lines. Over budget means unread.

     Rule of this file: point at a real file, do not describe it. An exemplar cannot go
     stale silently; a paragraph can. If there is no exemplar yet, the convention does not
     exist yet. Write the first one, then name it here. -->

## Structure

| thing | do it like | notes |
|---|---|---|
| route | `<path/to/exemplar>` | |
| component | `<path/to/exemplar>` | |
| data access | `<path/to/exemplar>` | |
| migration | `<path/to/exemplar>` | |
| test | `<path/to/exemplar>` | |
| background job | `<path/to/exemplar>` | |

## Tokens

<!-- Delete this section in a project with no interface. Filling it in switches the `tokens`
     gate on: colours and font stacks then live in `home` only, and a component carrying a raw
     hex fails. A single line may opt out with a `blueprint:allow-raw` comment.

     home: src/styles/tokens.css
     components: src/**/*.tsx, src/**/*.css -->

## Naming

<!-- Only rules that are decidable by looking. "Use clear names" is not a convention. -->

- files:
- exported symbols:
- db tables/columns:
- env vars:

## Errors

- shape:
- what the user sees:
- what gets logged (and what must never be logged):

## Never

<!-- Hard bans. Each one should be checkable by grep. -->

-
