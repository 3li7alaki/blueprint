# Product

<!-- budget: 60 lines. Written by the `look` pass, amended by `blueprint amend`, never by hand.

     This file is what a human said about who this is for and what it should feel like. It is
     read by every design tool in the repo, which is why it sits at the root rather than under
     blueprint/. DESIGN.md beside it is the opposite: generated from the code, describing what
     the interface actually is today. Stated versus derived, the same split as a requirement.

     An empty section is not a placeholder to fill in later. It fails the `look` gate, because
     an unanswered design question that nobody priced is exactly how a plausible but wrong
     product gets built. Run the grill, or record the refusal in OPEN.md with its cost. -->

## Register

<!-- `brand` (marketing, landing, campaign, portfolio: the design IS the product) or `product`
     (app UI, admin, dashboard, tool: the design SERVES the product). One bare word. -->

## Platform

<!-- `web`, `ios`, `android`, or `adaptive`. Decided by the design language the app renders,
     never by the toolchain. One bare word. -->

## Users

<!-- Who they are, where they are when they open this, and the job they came to do. Name a
     secondary audience only if one really exists. -->

## Product Purpose

<!-- What this does, why it exists, what success looks like. Problem, non-goals and success
     metric live in blueprint/PROJECT.md. Point at them, do not restate them. -->

## Positioning

<!-- One line. The claim every screen has to reinforce. -->

## Brand Personality

<!-- Voice and tone, three words, and the feeling someone should leave with. -->

## Anti-references

<!-- What this must not look like, named specifically. "Not generic" is not an anti-reference;
     "not the cream-and-serif AI landing page" is. -->

## Design Principles

<!-- Three to five, strategic, arguable. "Show the work, never the spinner" is a principle.
     "Use OKLCH" is a token decision and belongs in a decision record. -->

## Accessibility & Inclusion

<!-- Target level, known user needs, and anything the interface must never rely on alone. -->

## Conversion & proof

<!-- Brand register only. Delete the whole section, heading included, for a product. Primary
     and secondary CTA, the line remembered after ten seconds, the belief ladder, proof held. -->
